pipeline{
    agent any
    stages{
        stage("build & test"){
            steps{
                echo "======== ${env.STAGE_NAME} ========"
            }
        }
        stage("sonar"){
            steps{
                echo "======== ${env.STAGE_NAME} ========"
            }
        }
        stage("run"){
            steps{
                echo "======== ${env.STAGE_NAME} ========"
            }
        }
        stage("test"){
            steps{
                echo "======== ${env.STAGE_NAME} ========"
            }
        }
        stage("nexus"){
            steps{
                echo "======== ${env.STAGE_NAME} ========"
            }
        }
    }
}